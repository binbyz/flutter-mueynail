import 'package:flutter/material.dart';

class BookingTime extends StatelessWidget {
  const BookingTime({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(

    );
  }
}
